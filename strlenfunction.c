#include<stdio.h>
#include<conio.h>
#include<string.h>
main()
{
char s[50];
int l ;
printf("\nEnter first string: ");
gets(s);
l=strlen(s);

printf("\nLength of the String is: %d", l);
  getch();
}